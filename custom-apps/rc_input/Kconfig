config RC_INPUT_EXAMPLE
	bool "RC input example application"
	default n
	depends on RC_INPUT
	---help---
		Reads SBUS RC input and prints all channel values once per second.
		Useful for hardware bringup and verifying the rc_input pipeline.
